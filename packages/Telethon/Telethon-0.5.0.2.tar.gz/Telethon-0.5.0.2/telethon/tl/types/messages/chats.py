from telethon.tl.mtproto_request import MTProtoRequest


class Chats(MTProtoRequest):
    """Class generated by TLObjects' generator. All changes will be ERASED. Original .tl definition below.
    messages.chats#64ff9fd5 chats:Vector<Chat> = messages.Chats"""

    def __init__(self, chats):
        """
        :param chats: Telegram type: «Chat». Must be a list.
        """
        super().__init__()
        self.constructor_id = 0x64ff9fd5

        self.chats = chats

    def on_send(self, writer):
        writer.write_int(self.constructor_id, signed=False)
        writer.write_int(0x1cb5c415, signed=False)  # Vector's constructor ID
        writer.write_int(len(self.chats))
        for chats_item in self.chats:
            chats_item.on_send(writer)

    def on_response(self, reader):
        reader.read_int()  # Vector's constructor ID
        self.chats = []  # Initialize an empty list
        chats_len = reader.read_int()
        for _ in range(chats_len):
            chats_item = reader.tgread_object()
            self.chats.append(chats_item)

    def __repr__(self):
        return 'messages.chats#64ff9fd5 chats:Vector<Chat> = messages.Chats'

    def __str__(self):
        return '(messages.chats (ID: 0x64ff9fd5) = (chats={}))'.format(None if not self.chats else [str(_) for _ in self.chats])
