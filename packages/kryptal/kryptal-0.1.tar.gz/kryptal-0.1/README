Initialize dev environment:
----------------------
1) Create virtualenv
   $ virtualenv -p /usr/bin/python3.5 virtualenv
   $ source virtualenv/bin/activate

2) Install packages
   $ pip install -e .


Load dev environment:
-----------------------
Load virtualenv:
$ source virtualenv/bin/activate
