version: 1


database settings:
    remote database:
        db: XXX
        host: 127.0.0.1
        user: XXX
        password: XXX

# SSH TUNNEL - if a tunnel is required to connect to the database(s) then add setup here
ssh tunnel:
    use tunnel: True   # True | False
    remote user: XXX
    remote ip: XXX
    remote datbase host: localhost
    port: 9007



read:
    reading_list_root_path: XXX

gmail:
    smtp_server: smtp.gmail.com
    smtp_port: 465
    smtp_login: XXX
    user_email: XXX
    kindle_emails: 
        - XXX@kindle.com
        - XXX@kindle.com
    smtp_password: XXX
    convert: False

metadata import locations:
    - XXX

metadata import locations:
    - XXX

readability:
    parser api token: XXX
    reader api key: XXX
    reader api secret: XXX



logging settings:
    formatters:
        file_style:
            format: '* %(asctime)s - %(name)s - %(levelname)s (%(pathname)s > %(funcName)s > %(lineno)d) - %(message)s  '
            datefmt: '%Y/%m/%d %H:%M:%S'
        console_style:
            format: '* %(asctime)s - %(levelname)s: %(pathname)s:%(funcName)s:%(lineno)d > %(message)s'
            datefmt: '%H:%M:%S'
        html_style:
            format: '<div id="row" class="%(levelname)s"><span class="date">%(asctime)s</span>   <span class="label">file:</span><span class="filename">%(filename)s</span>   <span class="label">method:</span><span class="funcName">%(funcName)s</span>   <span class="label">line#:</span><span class="lineno">%(lineno)d</span> <span class="pathname">%(pathname)s</span>  <div class="right"><span class="message">%(message)s</span><span class="levelname">%(levelname)s</span></div></div>'
            datefmt: '%Y-%m-%d <span class= "time">%H:%M <span class= "seconds">%Ss</span></span>'
    handlers:
        console:
            class: logging.StreamHandler
            level: DEBUG
            formatter: console_style
            stream: ext://sys.stdout
        file:
            class: logging.handlers.GroupWriteRotatingFileHandler
            level: WARNING
            formatter: file_style
            filename: /Users/Dave/.config/headjack/headjack.log
            mode: w+
            maxBytes: 102400
            backupCount: 1
    root:
        level: WARNING
        handlers: [file,console]
