{% for repository, packages in repositories.items() %}{{ repository | cli('magenta') }}:
{% for package in packages %}    {{ package.name | cli(attrs=['bold']) }} {{ package.repository_version | cli('red') }} → {{ package.upstream_version | cli('green') }}
{% endfor %}{% endfor %}
