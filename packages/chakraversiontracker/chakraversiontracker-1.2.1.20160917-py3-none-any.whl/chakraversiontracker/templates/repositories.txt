{% for repository, packages in repositories.items() %}{{ repository }}:
{% for package in packages %}    {{ package.name }} {{ package.repository_version }} → {{ package.upstream_version }}
{% endfor %}
{% endfor %}
