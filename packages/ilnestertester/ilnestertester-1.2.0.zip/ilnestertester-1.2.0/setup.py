from distutils.core import setup

setup(
        name         = 'ilnestertester',
        version      = '1.2.0',
        py_modules   = ['nester'],
        description  = 'A simple printer of nested lists',
    )
