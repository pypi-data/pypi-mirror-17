# -*- coding: utf-8 -*-


def merge(elements):
    if len(elements) <= 1:
        # When the length of the list if less than or equal to 1, just return
        # the list back.
        return elements
