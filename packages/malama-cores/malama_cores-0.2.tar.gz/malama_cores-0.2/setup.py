from setuptools import setup, find_packages

setup(
    name = "malama_cores",
    version = "0.2",
    description = "Core libraries for malama",
    author = "andy zhang",
    license = "BSD",
    packages = find_packages(),
    classifiers = [ "Development Status :: 1 - Planning"]
)
