from distutils.core import setup

setup(
        name         = 'nestertester',
        version      = '1.1.0',
        py_modules   = ['nester'],
        description  = 'A simple printer of nested lists',
    )
