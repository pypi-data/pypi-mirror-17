#!/usr/bin/env python
import argparse
import logging

from zymbit.upstream.registration import write_auth_token

logging.basicConfig(level=logging.INFO)

parser = argparse.ArgumentParser()
parser.add_argument('auth_token')

args = parser.parse_args()

write_auth_token(args.auth_token)
